var express = require('express');
var router = express.Router();

//importing the message model lets us use mongoose and specifically the
//the message model


//var Message = require('../models/message');


module.exports = router;
